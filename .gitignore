node_modules/
dist/
*.log
.DS_Store
.env
.env.local
.relay-test/
coverage/

# Antigravity CLI (`agy`) drops a per-project symlink here on first open.
# Target carries absolute personal paths + `allowWrite:true` and must never
# reach a commit.
.antigravitycli/

# Next.js
.next/
out/
next-env.d.ts
*.tsbuildinfo
